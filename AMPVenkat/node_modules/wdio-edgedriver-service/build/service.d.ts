import type { Options, Capabilities } from '@wdio/types';
import type { EdgedriverServiceOptions } from './types';
export default class EdgedriverService {
    #private;
    constructor(options: EdgedriverServiceOptions | undefined, _: never, config: Options.Testrunner);
    onPrepare(): Promise<string>;
    beforeSession(_: never, capabilities: Capabilities.Capabilities | Capabilities.MultiRemoteCapabilities, __: never, cid: string): Promise<void> | undefined;
    afterSession(): void;
}
//# sourceMappingURL=service.d.ts.map